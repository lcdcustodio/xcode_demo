stages:
  - build

variables:
  LC_ALL: "en_US.UTF-8"
  LANG: "en_US.UTF-8"
  GIT_STRATEGY: clone

build:
  stage: build
  script:
    - ls
    - pwd
#    - bundle install
    - BUNDLE_GEMFILE="ios/Gemfile" bundle install
#    - bundle exec fastlane build

